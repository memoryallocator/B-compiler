a;
b 2;
c b;

vec [];
vec [20];
vec [] "some value", "another value";
vec [3] "first", "second", "third", "fourth", "fifth";
vec [5] "first", "second";

main() {
    {
        ;
    }
    return ( a++ + ++b + 0 + ~!*c++ + ++d + &e - --f + g + h-- * i ) ;
}
